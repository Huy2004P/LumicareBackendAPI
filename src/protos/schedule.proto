syntax = "proto3";

package schedule;

service ScheduleService {
  rpc BulkCreateSchedule (BulkCreateScheduleRequest) returns (ScheduleResponse);
  rpc GetScheduleByDate (GetScheduleRequest) returns (GetScheduleListResponse);
}

message ScheduleResponse {
  bool success = 1;
  string message = 2;
}

message BulkCreateScheduleRequest {
  int32 doctor_id = 1;       
  string date = 2;           
  repeated string time_types = 3; 
  int32 max_booking = 4;     
}

message GetScheduleRequest {
  int32 doctor_id = 1;
  string date = 2; 
}

message ScheduleItem {
  int32 id = 1;
  string time_type = 2;       
  string time_display = 3;    
  int32 max_booking = 4;
  int32 current_booking = 5;
  bool is_available = 6;      
}

message GetScheduleListResponse {
  bool success = 1;
  repeated ScheduleItem data = 2;
}